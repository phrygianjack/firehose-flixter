<br />
<div class="booyah-box col-xs-10 col-xs-offset-1 clearfix">
  <h1 class="text-center"><%= current_course.title %></h1>
  <%= image_tag(current_course.image, class: 'img-responsive') %>

  <br />
  <%= link_to 'Preview as Student...', course_path(current_course), class: 'btn btn-warning pull-right' %>
  <br class="clear" />

  <div class="text-center">
  	<%= link_to 'Add a new section...', new_instructor_course_section_path(current_course), class: 'btn btn-primary btn-large' %>
  </div>

  <ul>
  	<% current_course.sections.order(created_at: :asc).each do |section| %>
  		<li>
  			<%= section.title %>
  			<%= link_to 'New Lesson', new_instructor_section_lesson_path(section), class: 'pull-right btn btn-primary btn-xs' %>
  			<ul>
  				<% section.lessons.order(created_at: :asc).each do |lesson| %>
  					<li>
  						<b><%= lesson.title %></b>
  						-
  						<%= lesson.subtitle %>
  					</li>
  					<% end %>
  				</ul>
  			<br class = "clr" />
  		</li>
  	<% end %>
  </ul>
</div>